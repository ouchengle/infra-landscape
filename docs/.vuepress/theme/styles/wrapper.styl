$wrapper
  max-width $contentWidth
  margin 0 auto
  padding $navbarHeight - 1.4rem + $navbar-vertical-padding * 2 2.5rem
  @media (max-width: $MQNarrow)
    padding 2rem
  @media (max-width: $MQMobileNarrow)
    padding 1.5rem

